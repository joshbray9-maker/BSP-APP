# This preliminary build does not connect to a real backend yet — see docs/OPEN_QUESTIONS.md.
# These are placeholders for the milestone where Supabase is actually wired up.

# Client (bundled into the browser build — treat as public; RLS is the real guard)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Tooling only (NEVER prefix with VITE_, NEVER commit real values, NEVER paste into an AI prompt)
SUPABASE_PROJECT_REF=
SUPABASE_SECRET_KEY=
